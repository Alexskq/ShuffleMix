<h1 class="text-3xl sm:text-5xl font-bold text-center pt-16 pb-20 text-[#CA244D] ">🏆 Shuffle Selection 🏆 </h1>

<div class="md:flex md:justify-between md:items-start gap-2">
  <div id="dotted">
  <div class="card mx-auto md:w-96 self-center shadow-xl p-5  border border-[#FFF7F8]">
    <span id="headline" class="text-3xl font-bold mb-4 py-2 text-[#CA244D]">📜 Projets</span>
    <small id="subheader">(separated by commas or new lines) 
      <span id="counter"></span>
    </small>
    <textarea class="p-3 border border-[#F8BFC8] w-full rounded-s-md bg-inherit" id="names" rows="5" cols="25"><%= @participants.map(&:name).join(", ") %></textarea>
    <small id="counter"></small>
    </div>
  </div>

  <div class="flex gap-3 self-center card-body md:text-sm">
    <button id="spin" class="btn btn-active bg-[#46A758] sm:text-xl text-sm w-52 self-center text-white">Spin</button>
    <button id="remove" class="sm:text-xl text-sm w-52 self-center">Remove Winner</button>
  </div>

  <div class="h-full flex justify-center items-center">
    <div class="py-5 text-3xl font-bold mb-4 items-start card  w-96 shadow-xl p-5 text-[#cececf] border border-[#FFF7F8]">
      <h2 class="text-[#CA244D]">🥇 Ordres de passage</h2>
      <div id="passageOrder" class="font-normal mb-5 flex-col justify-start">
      </div>
    </div>
  </div>
</div>

  <div class="container flex justify-center my-10 py-10">
    <div id="wheel"></div>
  </div>

<!-- Modale pour afficher le gagnant -->
<div id="projectModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full hidden mx-auto my-0">
  <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
    <div class="mt-3 text-center">
      <h3 class="text-3xl leading-6 text-gray-900 font-bold">WINNER</h3>
      <div class="mt-2 px-7 py-3">
        <p id="winnerName" class="text-xl font-normal text-gray-700"></p>
        <p id="projectName" class="text-sm text-gray-500"></p>
      </div>
      <div class="items-center px-4 py-3">
        <button id="closeModal" class="px-4 py-2 bg-blue-500 text-white text-base font-medium rounded-md w-full shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-300">
          Fermer
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  // Liste des participants et leurs projets
  const participantProjects = {
    <% @participants.each do |participant| %>
      "<%= participant.name %>": "<%= participant.project_name %>",
    <% end %>
  };

  document.addEventListener('DOMContentLoaded', function() {
    const modal = document.getElementById('projectModal');
    const projectName = document.getElementById('projectName');
    const winnerName = document.getElementById('winnerName');
    const closeModal = document.getElementById('closeModal');

    // Fonction pour afficher la modale avec le nom du gagnant et son projet
    window.showProjectModal = function(winner) {
      const project = participantProjects[winner] || "Projet non spécifié";
      winnerName.textContent = `👉 ${winner}`;
      // projectName.textContent = `${project}`;
      modal.classList.remove('hidden');
    };

    closeModal.addEventListener('click', function() {
      modal.classList.add('hidden');
    });

    // Simulation du spin de la roue
    // document.getElementById('spin').addEventListener('click', function() {
    //   const names = Object.keys(participantProjects);
    //   const winner = names[Math.floor(Math.random() * names.length)];
    //   showProjectModal(winner);
    // });
  });
</script>
<%= javascript_include_tag 'custom' %>



  